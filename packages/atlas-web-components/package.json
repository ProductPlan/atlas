{
  "author": "ProductPlan <dev@productplan.com>",
  "name": "@productplan/atlas-web-components",
  "homepage": "https://github.com/ProductPlan/atlas",
  "license": "MIT",
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "yarn run build:lib",
    "develop": "# TODO: not implemented",
    "build": "# TODO: not implemented",
    "build:lib": "vite build -c=vite.lib.config.js",
    "build:lib:watch": "yarn run build:lib -w",
    "preview": "vite build && vite preview"
  },
  "dependencies": {
    "@productplan/atlas-tokens": "*",
    "preact": "^10.11.2",
    "preact-custom-element": "^4.2.1",
    "redefine-custom-elements": "^0.1.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.4.0",
    "terser": "^5.15.1",
    "vite": "^3.2.3"
  },
  "files": [
    "dist/"
  ]
}
