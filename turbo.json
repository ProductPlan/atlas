{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "build:watch": {
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "dependsOn": ["^dev", "^build:watch"],
      "cache": false
    },
    "test": {
      "outputs": []
    },
    "export": {
      "dependsOn": ["lint", "test", "build"],
      "outputs": []
    }
  },
  "globalEnv": ["NEXT_PUBLIC_BASE_PATH"]
}
